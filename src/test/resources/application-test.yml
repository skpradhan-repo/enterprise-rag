spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ragdb_test
    username: raguser
    password: ragpassword

  flyway:
    clean-disabled: false   # allow clean in tests

  ai:
    ollama:
      base-url: http://localhost:11434   # overridden by Testcontainers in integration tests
    vectorstore:
      pgvector:
        initialize-schema: false

logging:
  level:
    com.enterprise.rag: DEBUG
    org.springframework.security: DEBUG
